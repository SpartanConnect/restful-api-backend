var DATABASE={
    //Host, user, port, and password are all declared in the secret.js file.
    SQL_DB_NAME:"spartanconnect"
};

//Export the SQL info (except for pw, of course)
module.exports=DATABASE;
